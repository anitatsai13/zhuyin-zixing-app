---
title: 單檔 HTML 學習 App 製作方法（形音義辨識＋字音字形）
type: 知識庫
source: "https://anitatsai13.github.io/xingyinyi-app/ ・ https://anitatsai13.github.io/zhuyin-zixing-app/"
created: 2026-09-23
tags: [單檔HTML, 學習App, PWA, Supabase, 國文]
related: []
---

# 單檔 HTML 學習 App 製作方法

> 原始資料：xingyinyi-app（形音義辨識）與 zhuyin-zixing-app（字音字形）兩個 GitHub repo 的實際原始碼
> 整理日期：2026-09-23

兩個 App 採**同一套製作模式**：所有資料、程式、樣式全部內嵌在一個 `index.html` 裡（形音義 659KB、字音字形 377KB），離線可開、免安裝、免後端。以下先講共同架構，再列各自特色。

---

## 核心概念

### 1. 單一自包含 HTML（self-contained）

```
repo/
├── index.html          ← App 本體：資料＋程式＋樣式全內嵌
├── manifest.webmanifest ← PWA 安裝資訊（名稱、圖示、全螢幕）
├── sw.js               ← Service Worker（離線快取）
├── icon.svg            ← App 圖示（285 bytes 的極簡 SVG）
└── README.md           ← 修改與部署說明
```

`index.html` 內部結構固定為四段：

| 區段 | 內容 |
|---|---|
| `<head>` | meta（viewport、theme-color、apple-touch）＋一行 SW 註冊腳本 |
| `<style>`（約 100 行） | CSS 變數主題（`:root{--bg;--brand;…}`）＋元件樣式 |
| `<body>` 骨架 | sticky `<header>`（標題＋冊別/課別/分類 `<select>`＋分頁 `.tabs`）＋空的 `<div id="app">` |
| `<script>`（一大段） | ①巨型資料常數 ②正規化 ③測驗引擎 ④渲染 ⑤錯題本 ⑥雲端同步 |

### 2. 資料層：把講義變成 JSON 常數

- **資料建置管線放在 repo 外**（本機「建置工具」資料夾）：`parse*.py` 解析原始 .doc/.pdf → 各 .json → `build*.py` 注入產出最終 `index.html`。repo 只放成品。
- 資料以一個巨型常數開頭，例如：
  - 形音義：`const DATA=[{id:"1上|L01", title:"第一課 夏夜", cats:[{name:"形近字", groups:[{chars:["蜜","密"], entries:[{char,yin,disp[],ex[]}]}]}]}]`（冊|課 → 分類 → 字群 → 單字）
  - 字音字形：`const DATASETS=[{id:"fill", kind:"fill", lessons:[{lesson, items:[{word,char,yin,ask}]}]}, {id:"idiom", kind:"idiom", …}]`（62 課、字音字形 1732 題＋成語 1932 句）
- 載入後先跑一次**正規化 IIFE**（例：來源用漢字「一」代替注音「ㄧ」，統一置換，避免測驗選項看似重複）。

### 3. UI 層：無框架、原生 DOM

- 不用任何前端框架，`document.getElementById` ＋ template string 組 HTML。
- 三個 `<select>`（冊別→課別→分類）連動篩選範圍；`.tabs` 切換模式（瀏覽學習／各種測驗／錯題本），切分頁時整個 `#app` 重繪。
- 字體堆疊：內文 PingFang TC／Microsoft JhengHei，標楷體變數 `--kai` 用於生字展示。

### 4. 測驗引擎：誘答選項「挑易混淆」而非亂數

這是兩個 App 最講究的部分（多次 commit 迭代）：

| 題型 | 誘答策略 |
|---|---|
| 注音測驗／填注音 | 按「注音相似度」排序：**同音 > 同音異調 > 共用聲母或韻母**，取最像的 |
| 形近字選字 | 直接用資料裡同一組的形近字（本來就是易混淆組） |
| 填國字 | 加入同音、近音字 |
| 成語擂臺 | 以「**釋義相近**」的近義成語為主，共用字／字數只作次要加權 |

作答後即時判定、標色、記錄錯題。

### 5. 錯題本＋雲端會員（Supabase）

- 錯題先存 `localStorage`（key 如 `MKEY`），錯題本分頁可重測。
- 雲端同步用 **Supabase**（`SUPABASE_URL`＋公開 anon key 直接內嵌 HTML，靠 RLS 保護資料）：註冊/登入 → `cloudSave/cloudLoad` 同步錯題，`mergeArr` 合併本地與雲端、去重。
- 兩個 App **共用同一組 Supabase 專案登入**，以 `APPID` 區分資料歸屬（同帳號跨 App）。

### 6. PWA：三個小檔案讓它像 App

- `manifest.webmanifest`：名稱、`display: standalone`（全螢幕）、theme-color、icon.svg。
- `sw.js`（約 750 bytes，cache-first）：
  - `install`：預快取 `["./","./index.html","./manifest.webmanifest","./icon.svg"]`
  - `activate`：刪舊版快取（快取名如 `zhuyin-v1`，**改版要升版號**）
  - `fetch`：先回快取、沒有才上網並回填、離線 fallback 到 index.html
- head 一行註冊：`navigator.serviceWorker.register('sw.js')`。

### 7. 部署：靜態託管，拖檔即更新

- **GitHub Pages**：repo push 到 main 即自動部署（https://anitatsai13.github.io/<repo>/）。
- **Netlify**：把 `index.html` 拖進站台的 Deploys 頁即更新，網址不變。
- 因為是單檔靜態頁，兩邊可同時掛，無需 build step。

---

## 兩個 App 的差異速覽

| | 形音義辨識（xingyinyi-app） | 字音字形（zhuyin-zixing-app） |
|---|---|---|
| 範圍 | 國中國文 1上～3上 形近字／多音字 | 國小～國中 字音字形＋成語，62 課 |
| 分頁 | 瀏覽學習／形近字測驗／注音測驗／錯題本 | 學習／近似字／成語擂臺／字詞測驗（依資料集切換 TABS） |
| 資料主軸 | 字群（一組形近字＋各字注音釋義例詞） | 題庫題（word/char/yin/ask）＋成語句 |
| 誘答重點 | 相似讀音、同組形近字 | 相似讀音、同音近音字、近義成語 |

---

## 與我的教學的連結

- 同一套模式也用在「英文單字學習網」「英文文法學習網」：巨型 JSON 常數＋無框架渲染＋錯題本＋PWA＋GitHub Pages，只是資料結構換成單字/文法題。
- 要再做新科目 App 時的起手式：①用 python 把講義/題本解析成 JSON ②套這個 HTML 骨架 ③設計「易混淆」誘答規則（這是學習效果的關鍵）④加 PWA 三件套 ⑤推 GitHub Pages。

## 內容缺口（待補）

- [ ] 建置工具資料夾（parse*.py／build*.py）的實際位置與用法尚未整理
- [ ] Supabase RLS 規則與資料表結構未記錄
